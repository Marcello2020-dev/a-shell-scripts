# a-shell-scripts (PDF SHA-256 Workflow)

**Ziel:** In einem Ordner mit PDFs
1) SHA-256 Hash-Dateien erzeugen (`*.sha256.txt`),
2) Prüfung gegen die PDFs,
3) PDF-Liste mit Zeitstempeln erzeugen,
4) optional alles wieder aufräumen.

---

## Quick Start (a-Shell / iOS)

### 1) In den PDF-Ordner wechseln
In a-Shell:
```sh
pickFolder
pwd